export type userType = {
    name: string,
    login: string,
    email: string,
    location: string,
    avatar_url: string,
    url_profile: string
};

export type repositoryType = {
    name: string,
    description: string,
    language: string,
    stargazers_count: string,
    forks_count: string
};